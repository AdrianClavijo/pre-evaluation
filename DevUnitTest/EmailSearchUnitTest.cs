using DevTest.Controllers;
using DevTest.Models;
using DevTest.Services;
using Microsoft.AspNetCore.Mvc;

namespace DevUnitTest
{
    public class EmailSearchUnitTest
    {/*
        private readonly ContactController _controller;
        private readonly DB_DevTestContext _dbcontext;
        
        
        public EmailSearchUnitTest()
        {
            _dbcontext = new DB_DevTestContext();
 //           _controller = new ContactController(_dbcontext, _contactservice);
        }
        
        [Fact]
        public void GetEmail()
        {
            //Arrange
            string Email = "a@gmail.com";
            //Act
            var result = _controller.GetByEmail(Email);
            //Assert
            Assert.IsType<NotFoundResult>(result?.Value);
        }
        [Fact]
        public void Save()
        {
            //Arrange
            string Email = "a@gmail.com";
            //Act
            var result = _controller.GetByEmail(Email);
            //Assert
            Assert.IsType<NotFoundResult>(result?.Value);
        }
      */
    }
}